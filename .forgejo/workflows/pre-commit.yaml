---
name: Pre-Commit Checks

# yamllint disable-line rule:truthy
on:
  push:
    branches: [main]

jobs:
  call-reusable:
    runs-on: ubuntu-latest
    steps:
      - uses: https://git.zyria.de/pyrox/actions/pre-commit@v1
